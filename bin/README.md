# This is a /bin directory

This directory contains support scripts needed to maintain the documentation repository.

 - `convert_puml.sh` is a Bourne shell script used to convert in bulk the PlanUML diagrams into PNG for use in markdown documents.
 - `plantuml-1.2025.10.jar` is the current version of the plantuml conversion application.

